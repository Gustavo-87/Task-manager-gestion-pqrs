@extends('layouts.app') @section('titulo','Auditoría') @section('titulo_pagina','Auditoría administrativa')
@section('contenido')<section class="page-heading"><div><span class="eyebrow">Seguridad</span><h1>Registro de auditoría</h1><p>Acciones que modificaron información en la plataforma.</p></div></section><section class="panel audit-list">@forelse($logs as $log)<article><span class="audit-method">{{ Str::before($log->action,' ') }}</span><div><strong>{{ $log->action }}</strong><p>{{ $log->user?->name??'Sistema' }} · {{ $log->ip_address }}</p></div><time>{{ $log->created_at->diffForHumans() }}</time></article>@empty<div class="empty-state"><h3>Sin registros todavía</h3></div>@endforelse<div class="panel-footer">{{ $logs->links() }}</div></section>@endsection
