@extends('layouts.app')
@section('titulo','Mi perfil') @section('titulo_pagina','Mi perfil')
@section('contenido')<section class="page-heading compact"><div><span class="eyebrow">Cuenta personal</span><h1>Actualiza tu perfil</h1><p>Administra tus datos de acceso y seguridad.</p></div></section><form class="form-panel settings-panel" method="POST" action="{{ route('profile.update') }}">@csrf @method('PUT')<div class="form-intro"><span class="avatar">{{ Str::upper(Str::substr($user->name,0,2)) }}</span><div><h2>{{ $user->name }}</h2><p>Rol: {{ ucfirst($user->role) }}</p></div></div><div class="form-grid"><div class="field"><label for="name">Nombre</label><input id="name" name="name" value="{{ old('name',$user->name) }}" required></div><div class="field"><label for="email">Correo</label><input id="email" name="email" type="email" value="{{ old('email',$user->email) }}" required></div><div class="field span-2 profile-security"><h3>Cambiar contraseña</h3><p>Déjala vacía si deseas conservar la actual.</p></div><div class="field"><label for="current_password">Contraseña actual</label><input id="current_password" name="current_password" type="password" autocomplete="current-password"></div><div class="field"><label for="password">Nueva contraseña</label><input id="password" name="password" type="password" autocomplete="new-password"></div><div class="field"><label for="password_confirmation">Confirmar nueva contraseña</label><input id="password_confirmation" name="password_confirmation" type="password"></div></div><div class="form-actions"><div><button class="button primary">Guardar perfil →</button></div></div></form>@endsection
